package chapter10.lang;

public class SysMain {

	public static void main(String[] args) {
//		Sys.out.println("java");
		chapter10.Sys.out.println("java"); // import하지 않아도 앞에 패키지명 붙이면 사용 가능
		java.lang.System.out.println("java");
		System.out.println("java");
		// System은 java.lang 패키지에 묶여 있는 클래스의 이름
		// java.lang이나 import문 생략해도 사용 가능하도록 컴파일러와 약속되어있음
		// out은 클래스 System의 이름을 통해 접근하므로
		// 이는 System 클래스의 클래스 변수 이름임을 유추 가능
		// 인스턴스 생성없이 바로 사용 가능하여 static 붙어있는 클래스 변수라는 것도 유추 가능
		
		// 이클립스 내에선 italic체로 되어있기 때문에 클래스 변수라는 것을 알 수 있음
		// 이클립스 내에서 굵은 글씨체로 표현되어 final 키워드가 붙어있는 상수
		
		
		
	}

}
